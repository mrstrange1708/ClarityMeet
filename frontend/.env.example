# Backend API URL (only needed for production builds)
# In development, the Vite proxy handles /api → localhost:7777 automatically
# For production, set this to your hosted backend URL:
# VITE_API_URL=https://your-backend.onrender.com/api
